# Chief Project Context

> Generated by /chief-init on 2026-03-19. Updated 2026-03-19 (2). Edit freely — chief reads this every session.
> Re-run `/chief-init` to update detected values after major stack changes.

## Stack

| | |
|---|---|
| Language | TypeScript |
| Runtime | Bun (>=1.0.0) |
| Framework | None — CLI tool + AI skill prompts |
| Package manager | bun |
| Database | None |
| ORM / Query layer | None |
| Testing | bun test (custom suite: skill validation, gen-skill-docs, browse integration, E2E evals) |
| Linting / formatting | None configured |
| Deployment | Distributed as git repo — users clone to `~/.claude/skills/chief` and run `./setup` |
| CI/CD | GitHub Actions (`skill-docs.yml`) |

## Project Structure

- `browse/` — Headless browser CLI (Playwright). Core binary that powers all `/browse` commands.
  - `src/commands.ts` — Single source of truth for all browse commands
  - `src/snapshot.ts` — SNAPSHOT_FLAGS metadata
  - `dist/` — Compiled binary
- `scripts/` — Build + DX tooling (gen-skill-docs, skill-check, dev-skill, eval utilities)
- `test/` — Skill validation, E2E evals, LLM-judge tests, fixtures
- `bin/` — Shell helper binaries (chief-update-check, chief-config, chief-diff-scope, chief-review-log, chief-slug, chief-bump-version, etc.)
  - `bin/chief-bump-version` — bumps VERSION + package.json atomically (patch/minor/major); called by `/chief-push` version phase
- `chief-init/`, `chief-push/`, `chief-cook/`, `chief-upgrade/`, `chief-resolve/` — Chief-specific skills (the fork's new layer)
- `<skill-name>/` — All other skills (review, ship, qa, retro, investigate, etc.)
- `SKILL.md.tmpl` — Template source for the top-level skill; edit this, not SKILL.md
- `CLAUDE.md` — Project instructions for Claude Code sessions
- `CHIEF.md` — This file

## Project Context

| | |
|---|---|
| Type | Open source |
| Team size | Solo |
| Team levels | Solo |
| Current focus | Building chief-native features on top of the completed rebrand — version management, PR review resolution, quality gates. Branch: tadiwa/refactor-gstack-to-chief |

## Known Pain Points

**The rebrand is done, the PR is not merged yet.** The `tadiwa/refactor-gstack-to-chief`
branch has completed the gstack → chief rename across source files, templates, tests, and
bin scripts. `package.json` name and description are now chief. Until this lands on `main`,
`~/.claude/skills/chief` installs from the old main — the new chief-native features
(chief-resolve, chief-bump-version, Phase 4.5 version bump in chief-push) only exist on the branch.

**chief-resolve and version bumping are branch-only.** These features exist on
`tadiwa/refactor-gstack-to-chief` and haven't shipped to users yet. Don't treat them
as stable until the branch merges.

**No E2E evals for chief-native skills.** `chief-push`, `chief-init`, `chief-cook`,
`chief-resolve` have no automated E2E coverage. Regressions could go undetected.

## Engineering Conventions

From `CLAUDE.md`:

- **Always bisect commits.** Each commit is one logical change — rename/move separate from
  behavior changes, template changes separate from generated file regeneration.
- **SKILL.md files are generated.** Edit `.tmpl` files, run `bun run gen:skill-docs`, commit both.
- **bun test before every commit** (free, <2s). `bun run test:evals` before shipping (paid, diff-based).
- **CHANGELOG.md is for users**, not contributors. Lead with what the user can now DO.
- **Platform-agnostic skills** — never hardcode framework-specific commands. Read CLAUDE.md, ask if missing, persist the answer.
- **Skills must work without CLAUDE.md.** AskUserQuestion as fallback, then persist to CLAUDE.md.

Key commands:
```
bun test               # free tests — run before every commit
bun run test:evals     # paid evals — run before shipping (~$4/run max)
bun run gen:skill-docs # regenerate SKILL.md files from templates
bun run build          # gen docs + compile binaries
bun run skill:check    # health dashboard for all skills
```

## Chief Configuration

### Proactive Skill Suggestions
All in — suggest skills whenever relevant: `/review`, `/qa`, `/ship`, `/investigate`, `/retro`, etc.

### Coaching Focus Areas
Chief will pay attention to these during code reviews and coaching:
- **Skill template quality** — are changes to `.tmpl` files matched by a `gen:skill-docs` run?
  Generated files (`SKILL.md`) should never be edited directly.
- **Test coverage for new skills** — chief-native skills (chief-init, chief-push, chief-cook,
  chief-resolve) have no E2E evals. Flag opportunities to add coverage.
- **Commit bisection** — this project has strict atomic commit rules; flag kitchen-sink commits.
- **Version discipline** — `VERSION` and `package.json` must stay in sync. Use `bin/chief-bump-version`
  to update both atomically. Never edit one without the other.

### Skills to Prioritize
- `/chief-push` — quality gate before every push (includes version bump phase)
- `/chief-resolve` — work through PR review comments before merging
- `/review` — pre-landing PR review
- `/ship` — when ready to merge
- `/retro` — weekly to track chief's own evolution
